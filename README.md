# File-Automatic-Backup-System-
Overview

The Automatic Backup System is a Python-based application that automatically creates backups of important files and folders. It ensures data safety by generating timestamped backups and verifying file integrity using MD5 checksum validation.
